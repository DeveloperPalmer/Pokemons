plugins {
    alias(libs.plugins.anvil)
    id 'android-library-convention'
}

dependencies {
    api project(":core:domain")
    api project(":feature:auth:domain")

    implementation libs.dagger
    kapt libs.daggerCompiler
}

android {
    namespace 'com.sla.feature.login.domain'
}